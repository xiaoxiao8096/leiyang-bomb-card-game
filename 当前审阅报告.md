# 《耒阳炸弹》当前版本只读审阅报告

> 审阅范围仅限当前的 `耒阳炸弹.html` 与实际运行画面；本次**未修改游戏代码**。报告中的“应修复”是建议，不是已执行的改动。

## 总结判断

游戏已经不再是一个只有规则和牌桌的原型。当前版本有完整的双副牌规则引擎、抽牌分队、抓分结算、角色头像、成长资产、皮肤、公共聊天、头像道具、AI 气泡、多个玩法入口和音画特效。视觉与功能量已经很大，甚至超过一个普通本地扑克小游戏。

但你觉得“不对劲”是对的。根源不是内容不够，而是**规则核心、产品边界、界面层级和代码结构开始相互拉扯**。最优先的事情不该继续加道具或特效，而是先把牌局终局、先手、AI 牌型决策和主界面信息架构收紧。否则每继续添加一轮功能，后面修一处就更容易影响三处。

## 本次实际抽样

我在当前页面启动了一局炸弹，并用同花 50K 作为首手。公开记录中出现了：

| 顺序 | 公开出牌 |
| --- | --- |
| 玩家 | 同花 50K |
| Bocchi | 4 张炸 |
| Naomi | 5 张炸 |
| Naomi 获得牌权后 | 3 连对 |

因此，**当前 AI 并不是所有时候都只会领单张**；它能够打出炸弹和连对。用户感受到的“AI 总是出单张”更像是策略分支在某些局面下不稳定，或者长组合被保护逻辑过滤，而不是候选牌型生成完全失效。

## 优先级 P0：先修，否则会直接伤害本地规则可信度

| 问题 | 证据 | 为什么严重 | 建议方向 |
| --- | --- | --- | --- |
| **双下后的唯一补刀机会没有严格收束** | `play()` 在前两名同队时仅设置 `pendingSettle`，随后仍让剩余玩家按正常轮转继续出牌；规则弹窗则写着“下一位只有一次压牌吃分，队友不能继续跟压”。 | 同一件事的文字规则和实际状态机不一致，玩家无法判断当前到底是否结束。 | 单独建立“末墩补刀”状态：限定唯一可行动座位、限定一次合法压牌或不出，之后立即结算。 |
| **第三、第四名同队的直接终局没有落实** | `g.order.length === 3` 时无条件进入 `pendingSettle`，没有先判断第三名与第四名是否同队。 | 这会违背此前确认的“第三、第四同队不再争”的本地规则。 | 在第三名出完时先比较最后两人的队伍；同队直接结算，异队才开放一次补刀。 |
| **双下后的下一局先手不符合已确认规则** | `nextBtn` 使用 `g.order.at(-1)` 作为下一局先手；它不是“被双下方随机一人”。 | 这会直接影响下一小局的公平性和用户明确强调过的本地规则。 | 若本局是双下，先手从被双下队仍在座位中的两名角色随机选取；其他情况才使用上一局最后出完者。 |

## 优先级 P1：决定“AI 看起来聪不聪明”的问题

| 问题 | 当前情况 | 用户体感 | 建议方向 |
| --- | --- | --- | --- |
| **AI 领出仍是固定优先级，不是整体残局规划** | `aiChoice()` 依次优先三顺、连对、单顺、三张、对子、单张。 | 有长牌就出长牌是对的，但它不比较“出完后剩余手数”、下一手是否更碎、是否把分牌送给对方。 | 用“剩余手数最小化 + 分牌暴露成本 + 牌权风险 + 队友残局”综合评分，而不是硬编码分类顺序。 |
| **50K / 王炸保护过于宽** | `protectedBombIds()` 会保护所有已组成 50K 的 5、10、K，以及两张以上王。 | AI 可能为了不拆 50K，放弃更好的顺子、连对或收尾路径，显得拘谨。 | 只在“炸弹价值大于拆分收益”时保护；残局或能明显减少手数时允许拆。 |
| **AI 配合主要是文案，不是牌局协作** | 气泡与公共消息会说“我不抢你的牌权”等，但出牌只知道队伍、手数、报单和少量桌面分。 | 看起来像 AI 在聊天，实际仍经常没有真正的让牌、送牌、封单配合。 | 明确三种协作动作：让队友领、给队友送可接牌型、敌方报单时封住其可能出口；并让对话从实际决策原因生成。 |
| **对局信息使用原则不统一** | AI 不读取敌方精确手数，这是正确的；但队友只用手数，没有结合公开出牌轨迹推测结构。 | AI 看起来会“保守”，却不像真正会算牌。 | 维护公开牌计数与已出现牌型；对队友只基于公开信息推断，不读取其隐藏手牌。 |

## 优先级 P1：用户已经能直接看见的界面冲突

| 问题 | 当前实际状态 | 后果 | 建议方向 |
| --- | --- | --- | --- |
| **牌桌仍是大椭圆，不是已讨论的圆角竞技桌** | 运行时 `border-radius` 实际为 `200px / 120px`。 | 左右座位和背牌容易处在弧线的视觉边缘；用户已经明确觉得两侧牌显示不全。 | 采用圆角矩形或降低横向弧度，并把左右座位与背牌放进安全区，不要依赖百分比锚点硬挤。 |
| **超长已出牌区域回归为不换行** | `#playedBox` 当前是 `flex-wrap: nowrap`。 | 长顺、长连对、七八张炸会再次横向挤压；这正是用户之前反复提出的问题。 | 改为可控网格或 `flex-wrap: wrap`，同时给每一行固定的重叠规则。 |
| **机械阵营词仍留在实际页面** | 抽牌面板会渲染“本队 / 对队”；上方角色有永久“队友”徽章；顶部初始占位里也有“对手”。 | 与“只用角色名，不要队友/敌人等机械分类”的要求冲突。 | 分队阶段只显示角色名和抽到的牌；对局阶段用座位、状态、已报单和行动高光传达关系，去掉永久阵营词。 |
| **消息面板默认只有 0.82 不透明度** | `.chat-panel` 静置时半透明，悬停才变实。 | 消息本身是重要信息，却在不操作时主动变淡，视觉优先级不合理。 | 保持可读的不透明度；用更弱的边框和字体层级来降噪，而不是降文字可见性。 |
| **全屏特效视觉上过度抢桌面** | 全屏层虽设置 `pointer-events: none`，但 `z-index: 3000` 且使用整屏强背景。 | 不会挡点击，但会挡“看牌”和“读桌面”；连续触发时函数会取消旧定时器并重开动画，容易产生跳变。 | 大特效只给炸弹、终局和极高分墩；普通收分改为中央局部特效。增加明确队列或冷却，而非只取消上一次计时器。 |

## 优先级 P2：产品范围与维护性问题

### 1. 产品已经从“耒阳炸弹”变成五玩法大厅

当前单文件同时包含炸弹、斗地主、跑得快、长沙麻将和转转麻将。它们共用游戏豆、底注、皮肤、角色库、聊天、设置和记录。这个方向不是错，但它需要被当作“棋牌大厅”来设计；如果目标仍是把耒阳炸弹做扎实，这四个额外玩法正在稀释重点。

尤其是 `profile.matches`、`profile.wins` 和游戏豆被多个玩法共同修改，炸弹页的“总场次 / 胜率”不再天然只代表炸弹。底注 `base` 也由多个玩法共享，切换斗地主或跑得快后会影响炸弹的底注。若这是有意的大厅经济系统，需要在界面上明确说明；若不是，应拆开各玩法的统计和默认底注。

### 2. 单文件已经进入“补丁覆盖”状态

当前文件约 4,190 行，桌面样式、动画、皮肤和不同玩法样式存在多轮重复定义。例如已出牌卡片、牌桌圆角、聊天面板、座位信息分别在多个位置被再次覆盖。这样的结果是：

> 页面最终长什么样，不再由一处设计决定，而是由“最后一条恰好生效的 CSS”决定。

这正是“我明明改成圆角桌，运行时怎么还是椭圆桌”的根因。下一轮不应该继续在末尾追加覆盖样式，而应把炸弹模式自己的样式收成一个明确的最终区块，再把大厅与其他玩法隔离。

### 3. 聊天与 AI 气泡没有上限

`addChat()` 每次都不断追加 DOM 节点，没有消息数量上限。AI 对话、快语、道具反击和系统消息一起跑时，长局会不断增大消息区 DOM。建议只保留最近 60–100 条，并把相近的 AI 反击合并或限频。

### 4. 未激活玩法页面有空头像资源

当前页面检测到若干未激活玩法中的头像 `<img>` 没有 `src`。这不会影响正在玩的炸弹，但会让资源检测产生噪声，也反映出多玩法页面在初始状态并非完全独立。建议未启动玩法不要渲染空头像，或给统一占位图。

## 建议的处理顺序

| 阶段 | 只做什么 | 不要做什么 |
| --- | --- | --- |
| **第一步：规则止血** | 修终局补刀、第三四名同队、双下后先手。 | 不改皮肤、不加新道具。 |
| **第二步：AI 重做** | 把领出从固定优先级改为整体评分；补公开算牌与队友协作。 | 不用更多聊天文案掩盖决策问题。 |
| **第三步：炸弹模式 UI 收口** | 定死牌桌形状、已出牌换行、消息位置、角色命名规范。 | 不继续向同一屏叠加新功能。 |
| **第四步：大厅化或聚焦化二选一** | 决定这是“耒阳炸弹专属游戏”还是“本地棋牌大厅”。 | 不继续让多个玩法共享却不解释统计和底注。 |
| **第五步：技术整理** | 拆分数据、规则、渲染、样式与各玩法状态；至少把最终 CSS 集中。 | 不再用多段末尾覆盖修布局。 |

## 结论

当前版本的长处是：**规则骨架、随机角色、互动感、皮肤与单机可玩性已经齐全**。真正拖后腿的是“核心玩法还没有先收稳，外围系统已经长得很大”。

最值得先做的不是继续扩展内容，而是用一次小范围、可验证的重构，让炸弹模式在以下四点上重新可信：**终局严格符合本地规则、AI 领出有完整牌型思维、长牌不会挤出桌面、界面不再保留机械阵营词**。这四点稳住后，后续加语音、道具、皮肤才会是加分，而不是掩盖问题。
